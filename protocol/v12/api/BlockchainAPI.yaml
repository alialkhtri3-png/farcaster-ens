openapi: 3.0.0

info:
 title: SIP Identity Blockchain API
 version: 12.0.0

paths:

 /identity/block/{did}:
  get:
   summary: Resolve identity block

 /identity/verify:
  post:
   summary: Verify proof of identity

 /network/status:
  get:
   summary: Blockchain network status
