openapi: 3.0.0

info:
  title: SIP Runtime API
  version: 2.1.0

paths:

  /resolve/{did}:
    get:
      summary: Resolve DID identity

  /credential/{id}:
    get:
      summary: Resolve credential

  /health:
    get:
      summary: Protocol health
